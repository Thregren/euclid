# 开发进度

## M1 · 工程骨架与瓦片渲染 ✅

- SwiftPM 多目标工程：`TileKit`（核心库）、`EuclidApp`（应用）、`TileKitCheck`（自检）
- 投影：Web Mercator ↔ WGS84，瓦片编号与像素坐标三向互转，往返误差 < 1e-9
- 数据层：目录型数据源，`<z>/<x>/<y>` 与 `<z>/<y>/<x>` 自动嗅探，XYZ / TMS 行号可配，
  跟随符号链接，不做全量扫描；LRU 内存缓存 + 并发解码闸门 + 缺片负缓存
- 渲染：Core Animation 图层金字塔，每瓦片一个 CALayer，平移缩放只改 transform
- 交互：拖动平移、双指滚动平移、滚轮/⌘滚动/捏合缩放（锚点跟随指针）、双击放大、⌥双击缩小、
  方向键平移、⌘= / ⌘- / ⌘0 / ⌘⇧0
- 界面：三栏结构（侧栏 + 画布 + 检查器）、工具栏、底部状态栏、比例尺、缩放控件、空状态

## M2 · 坐标与视图信息 ✅

- 指针坐标：度分秒、十进制度、瓦片 `z/x/y`、瓦片内像素、墨卡托米坐标
- 一键复制坐标；输入经纬度跳转到指定位置
- 检查器实时显示中心坐标、缩放层级、分辨率、视口瓦片数、已缓存与缺片数
- 拖放打开：把文件夹拖到窗口上即可打开；侧栏记录最近打开的目录

## M3 · 测距 ✅

- 折线测距：点击加点、双击/回车结束、⌫ 撤销、Esc 取消
- WGS84 椭球 Vincenty 反向公式，每段给出长度、方位角、罗盘方位与转角，另有总长与起终点直线距离
- 顶点吸附（12 px 内吸附到已有顶点）、按住 Shift 约束水平/垂直/45°、按住 ⌘ 关闭吸附
- 任意顶点可拖动微调（草稿与已完成测量都支持）

## M4 · 测面积与导出 ✅

- 多边形测面积：周长、测地面积、闭合差，支持顶点拖动
- 点坐标工具：单击取点并读取坐标
- 导出 GeoJSON / KML / CSV，或直接复制到剪贴板
- 多条测量并存，各自配色，可选中查看分段明细、单独删除或全部清除

## M5 · 精修 ✅

- 画布配色全部改为语义色，切换深浅色时刷新图层颜色
- 应用图标（ICNS）、Info.plist 元数据、ad-hoc 签名打包
- 工具提示条、空状态、检查器分区整理

## M6 · 画圆、样式与 Excel 导出 ✅

- **浏览手势**：鼠标滚轮缩放，鼠标中键拖动平移（任何工具下都可用），左键拖动与触摸板双指滚动平移，
  触摸板捏合缩放；滚动量的缩放指数收敛到一个命名常量，便于调手感
- **画圆**：`MeasurementKind.circle` + `MapTool.circle`（O / ⌘5）。点圆心、再点一次定半径，
  拖「半径点」随时改半径，或在检查器里输入精确半径（米 / 公里）
  - 圆心与半径点存为两个顶点，半径取两者的 WGS84 测地距离
  - 新增 Vincenty 直接公式 `Geodesy.destination(from:initialBearing:distance:)`，
    圆周按 360 点测地采样，周长与面积都按同一组采样环计算，画出来的和算出来的一致
  - GeoJSON / KML 导出为 180 点闭合多边形，半径写进属性
- **逐条样式**：`MeasurementStyle`（描边颜色、描边粗细、填充颜色、填充不透明度）
  - 颜色是可选的覆盖值，没改过就继续用调色板语义色（仍随深浅色切换），改过就固定为 sRGB 分量
  - 检查器里用 ColorPicker + 滑块编辑，可「应用到全部」或「恢复默认」
  - 连续拖动滑块 / 连着调色只记一次撤销（撤销点合并窗口 1.5 s）
  - 旧存档缺 `style` 字段时手工解码回落默认样式
- **Excel 导出**：零依赖生成 `.xlsx`（`XLSX.swift`）
  - 自带一个只做「存储」的 ZIP 打包器与 CRC32，OOXML 允许不压缩存放成员，省掉 deflate 实现
  - 四张表：测量汇总、点坐标（含 Web Mercator 东/北坐标）、分段明细、说明
  - 自带 `ZIP.parse` 反解器，自检里逐条校验 CRC，保证产出的确是能被 Excel 打开的包

## M6.1 · 缺陷修复 ✅

两个都是「换层级」引起的渲染问题，靠 `EUCLID_TRACE_VIEW=1` 打出的每帧数字定位：

- **切换缩放层级后瓦片整屏消失**：渲染层维护了一个「请求过就不再请求」的集合，
  而换层级会把不再需要的图层整批移除；回到该层级时图层是新建的、图片却不会再取，
  只剩空的占位图层，整屏看起来就是「瓦片都没了」。
  现在判断依据改为「这一帧还缺图 + 没有在途请求 + 不是已确认缺片」，
  已取过的图片由 `TileProvider` 的内存缓存直接命中，不再有这道永久门槛
- **缩放到上下限后继续滚动会平移视图**：`zoomed(by:anchorViewPoint:)` 先按未夹取的层级
  计算锚点补偿、再 `clamped` 只修正比例尺，于是到达极限后每一格滚动都变成一次平移，
  几步就把影像推出画面（实测每步中心漂移约 1.3e-4 世界单位，而数据集只有 1.8e-4 宽）。
  现在把目标层级先夹到范围内再算补偿，层级不变时直接返回原相机

附带改进：取图队列在有并发额度空出时自动补跑，不再依赖用户继续操作。
自检相应新增 13 项（151 项总计），覆盖缩放极限处的连续缩放与锚点补偿。

## M6.2 · 消除加载与换层级时的闪烁 ✅

用户反馈「加载起来一闪一闪」，用 `EUCLID_TRACE_VIEW=1` + `EUCLID_DEBUG_SNAPSHOT` 逐帧定位到三个原因，
都在「图还没到位的那一瞬间」：

- **渲染空转**：兜底逻辑对「自己没有图、祖先也没有图」的格子每一轮都重新找一遍，
  找不到就再补跑一次同步，形成没有终止条件的回路。打开数据集时相机还停在数据之外，
  整个屏幕都是这种格子，实测同一帧日志刷了 2400 行、每秒两百多轮全量重绘
  （每轮都要重铺图层、重画标注、把 viewport 重新推给 SwiftUI）。
  现在把这类格子记进 `unresolvedTiles`，只在仍留在屏幕上时保留结论，候选集合只减不增，
  回路几轮内收敛：同样的打开流程从 2076 次同步降到 27 次
- **换层级蒙灰**：新瓦片图层原先先刷一层 35% 透明的占位色，而且插在保留的上一层影像**上面**，
  跨层级瞬间整屏像蒙了一层灰白纱（逐帧实测：影像最暗值从 65 被抬到 136，并出现大片 183 的平色），
  等图从磁盘解码完才恢复。现在新图层保持透明、由上一层影像顶着，自己的图到了再淡入；
  同一套缩放脚本下，修复前 4 个换层级帧全部发灰，修复后均为正常影像
- **首次落图硬贴**：瓦片按解码完成的先后上屏，一屏几十块「啪、啪」跳出来；
  现在首次出现也走 120 ms 淡入
- 顺带：数据范围算出来之前不再铺图。此前会停在与数据无关的位置铺一屏空占位并白读磁盘，
  范围一到画面整体跳一次；现在这期间画布保持空白，状态栏显示「正在读取数据范围…」

## M7 · 在线瓦片下载 ✅

按范围与层级把在线瓦片取到本地目录，落成 `<z>/<x>/<y>`，下载完可以直接用本程序打开离线浏览。

- **取图核心（`TileKit/TileSource.swift`、`TileKit/TileDownloader.swift`）**
  - 瓦片源就是一个 URL 模板：支持 `{z}` `{x}` `{y}`，另有 `{-y}`（TMS 行号）、
    `{s}`（子域轮转）、`{key}`（密钥按查询值转义）。因此 XYZ 直连、`{z}/{y}/{x}` 顺序、
    WMTS KVP（把 `TILEMATRIX={z}&TILEROW={y}&TILECOL={x}` 写进模板）都用同一套代码
  - 计划只保存每层的行列范围（不逐个存瓦片），东、南边界取「覆盖到即算」并用 1e-9 瓦片的容差
    吸收归一化坐标往返误差，贴着瓦片分界线的范围不会多取一圈
  - 下载器：并发上限 + 每秒请求数节流，404/410 记为缺片不重试，5xx/429/超时按指数退避重试，
    已存在的瓦片默认跳过，随时可取消；落盘用原子写，完成后写 `source.json` 与 `attribution.txt`
  - 取图通道抽成 `TileFetching` 协议，自检注入假实现，不依赖网络
- **界面（`EuclidApp/TileDownloadModel.swift`、`DownloadSheet.swift`）**
  - 工具栏「下载在线瓦片」/ ⌘⇧D；预设天地图（影像、影像注记、矢量，需要 tk）与 OpenStreetMap，
    另有「自定义模板」；模板带扩展名时自动用它作落盘后缀
  - 区域可来自当前视图 / 数据范围 / 手输经纬度，层级用步进器调节，实时给出预计瓦片数与粗略体积
  - 进度面板显示完成数、速度、预计剩余与已落盘体积；结束后可直接「打开这个数据集」
  - 状态栏在下载期间显示 `下载 x/y`
- **刻意不内置 Google 地图**：其条款不允许把瓦片抓取到服务之外保存，内置预设等于替用户做违规操作；
  需要别的源时可在「自定义模板」里填自己有权使用的源
- 自检新增 54 项（212 项总计）：范围到行列的换算、边界对齐不多取、模板渲染与密钥、缺片/重试/跳过/并发/取消、
  进度单调、落盘清单、以及「下载目录能被嗅探成数据集」

## M8 · 在线底图浏览与代码整理 ✅

- **取图通道抽象（`TileKit/TileImageSource.swift`、`RemoteTileSource.swift`）**
  - `TileImageSource` 协议只要求「给我这一格字节」；`DirectoryTileSource`（磁盘）与
    `RemoteTileSource`（URL 模板 + 密钥）各实现一份，`TileProvider` 只认协议
  - 于是在线底图与本地数据集走完全相同的渲染、缓存、并发与缺片处理，
    渲染层不再需要「是磁盘还是网络」的分支；顺带把 UA / Accept 与重试退避收敛到一处
- **在线底图（侧栏 / 工具栏切换）**
  - 底图与下载共用同一套源参数（预设、模板、密钥），避免两处选择不一致：
    `download.sourceID` 是唯一真源，切换预设时正在显示的底图跟着刷新
  - 在线底图按通用地图约定显示（256px 瓦片铺满自身像素数），Retina 与普通屏视觉比例尺一致；
    本地影像仍按设备像素 1:1，保证判读不糊
  - 画布装配收敛成一个 `SourceSetup`（取图器、瓦片边长、层级范围、适配范围、是否等待范围），
    本地与在线各自只负责填参数
- **空闲预取**：视野安定后预取外圈瓦片（单次上限 24 片，本地走磁盘、在线走网络），
  用「层级 + 视野行列」做键避免重复；只在首屏没有待取瓦片时做，不抢并发额度
- **下载面板新增**：复制范围 / 用剪贴板范围（`西,南,东,北`）、复制瓦片清单（`z,x,y`）
- **整理**：状态栏由两套分支合并成一套读数布局；检查器新增「在线底图」分区；
  去掉未使用的 `TileDownloadSummary.attempted`、`GeoBounds.center`；补 20 项自检（232 项总计）

## M9 · 按 HIG 打磨界面 ✅

用 `apple-design` 技能做了一轮全项目界面评审：先按技能的路由定位层级（基础层的排版、颜色、材质、
布局、动效、无障碍 + 组件层的工具栏、边栏、分组框、进度指示），再用 `scripts/fetch_hig.py`
拉取「针对 macOS 设计」「工具栏」「边栏」「加载」四页原文核对，最后逐条量化落地。

- **排版**：写死的 11pt 全部换成系统语义字号（macOS 同为 11pt，但会跟随系统文本大小），
  层级数字去掉圆体设计以收敛字样数量；小字不再用三级灰（HIG：≤17 点文字对比度至少 4.5:1）
- **控制层材质**：浮在地图上的缩放控件、比例尺、工具提示在 macOS 26+ 用 Liquid Glass
  （`glassEffect(.regular)`），更早系统退回标准材质 + 描边，保证「控制层浮在内容层之上」的层级一致
- **工具栏**：11 项减到 8 项、2 个分组，全部用符号并补齐 `.help`；「清除测量」并入测量菜单，
  「瓦片网格」并入底图菜单；窗口标题改为当前底图/数据集名（HIG：不要用 App 名当标题）
- **边栏**：把「打开瓦片目录」从边栏底部移到「数据源」标题旁（HIG：避免关键操作放边栏底部）
- **加载**：扫描目录与读取数据范围时在画布上显示进度指示与说明（HIG：加载别只留空白）
- **动效**：尊重「减少动态效果」，开启时跳过瓦片淡入与背景淡出
- **无障碍**：画布给出可读名称与当前层级；浮层按钮补无障碍标签
- **容器**：下载面板区块改用系统 `GroupBox`，深浅色、增强对比度、降低透明度交给系统

刻意保留的一处取舍：底部信息条（坐标 / 比例尺 / 层级 / 瓦片数 / 下载进度）。HIG 说 macOS
不要把关键信息放窗口底部，但 Finder、预览等系统 App 都有底部信息条；这里控件已全部移出底栏
（下载的取消入口在面板里），且同样的读数在检查器里各有一份，窗口被拖到屏幕外也不会丢信息。

## M10 · 图层叠加与画布重构 ✅

- **`TileLayerStack`（新）**：把「一个数据源 ↔ 一个宿主图层」的整套逻辑抽出来——
  换层级留旧图兜底、祖先贴图、缺片负缓存、并发取图、空闲预取、瓦片网格、无障碍命名，
  原先散在 `TileCanvasNSView` 里的 500 多行随之搬走，画布从 1289 行降到 780 行
- **两条图层栈**：画布同时挂「在线底图（下）」与「本地影像（上）」，共用同一个相机；
  每层按自己的瓦片像素密度从相机换算出数据层级（在线按地图约定、本地按设备像素 1:1），
  所以混用时各自的清晰度与层级编号都不会被对方带偏
- **不透明度**：两层各一条滑杆（侧栏「图层」），检查器「图层」分区显示当前来源与百分比；
  典型用法是把本地影像调到 50% 左右，透过正射影像核对底图路网的位置
- **装配去抖**：本地层与在线层各自记录「已装配」签名，切换底图不会顺带把本地影像重取一遍；
  只有数据集真的换了才重装
- 验证：合成了一套洋红色瓦片当在线底图，本地影像 55% 叠加后截图核对（合成、透明度、
  两层各自的取图与日志都对）；带叠加层再跑一轮 `i3,o3` 缩放脚本，14 帧全部有内容，无空白/蒙层帧

## M11 · 坐标基准对齐（GCJ-02 / BD-09）✅

技术路线里一直挂着这个风险点（§3.4）：国内不少底图不在 WGS84 上，高德 / 腾讯用 GCJ-02
（城区偏 100–500 m）、百度用 BD-09，而 WebODM 的正射影像在 WGS84 上，
两者直接叠加会整体差几百米，配准核对与读数都被带偏。现在把基准当成**数据源的属性**做掉。

- **算法（`TileKit/Datum.swift`）**
  - `Datum` 三档：WGS84 / GCJ-02 / BD-09，提供 `fromWGS84` / `toWGS84` 双向互转
  - 正向是公开流传的多项式拟合（Krasovsky 椭球 + 三角函数扰动），反向用前向公式迭代逼近，
    6 轮即收敛到厘米级；境外坐标不做偏移，与各家实现一致
  - 单测与公开实现逐位对照：WGS84 (116.404, 39.915) → GCJ-02 (116.410244, 39.916404)；
    境外（东京 / 伦敦 / 纽约）恒等；境内偏移量级在百米级且不超过 1 km
- **叠加对齐（`TileLayerStack.datum`）**
  - 取图行列按「把相机中心移回该基准」后算，于是拿到的是该基准网格下的瓦片编号；
    图形再按 `worldOffset` 落位，两者合起来就是「按基准偏移平移」
  - 偏移取视图中心处的线性近似，米制写法同时进日志与界面提示
  - `EUCLID_TRACE_VIEW=1` 的每帧日志会多打一段 `基准偏移=(东 492m, 北 360m)`，便于核对
- **下载也认基准（`TileDownloadPlan`）**：计划里记录基准，范围四个角换算后再取行列，
  落盘清单与 `source.json`、`attribution.txt` 里都写明基准
- **界面**：侧栏「底图」里多一个「坐标基准」选择器（带说明与偏移提示），
  检查器「在线底图」分区显示当前基准与对齐偏移，切换基准时状态栏给出方向与量级
- **顺带修掉一个缺陷**：在线图层的装配签名原先只含「模板 + 密钥」，
  于是只改基准时图层不重装、画面仍是旧偏移；现在签名带上基准
- 自检新增 57 项（269 项总计），覆盖互转可逆性、境外恒等、与公开实现对照、
  世界坐标偏移与米制偏移一致、偏移基准下的下载计划编号

真机核对：把本地数据集本身用一个只读 HTTP 服务当在线底图（两层同源，`EUCLID_DEBUG_DATUM`
指定基准、`EUCLID_DEBUG_LOCAL_OPACITY=0.5` 做叠加），WGS84 下两层完全重合；
切成 GCJ-02 后在线层整体向东北重影，逐帧日志 `基准偏移=(东 492m, 北 360m)` 与
公开算法在华东一带的量级一致；再跑一轮「先开底图、运行中切基准」，图层重新装配、
画面随即偏移（`frame-91`），确认上面那个缺陷确实修掉了。

## M12 · 出图与测量性能 ✅

### 性能：测量结果不再每帧重算（`TileKit/Measurement.swift`）

画布每次同步（平移、缩放、换层级、滚轮）都会重铺测量标注，检查器也会在
读数变化时重取每一条测量的结果。而一次求值要走几十次 Vincenty 反向公式，
圆更狠：360 点测地采样 + 周长 + 面积，合计上千次测地运算——
**这些数字只跟顶点有关，跟相机无关**，之前却在每帧重算。

- `MeasurementCalculator.evaluate(kind:points:)` 带缓存，键就是「类型 + 顶点序列」；
  圆的度量另按「圆心 + 半径」存一份，连 360 点采样一起复用
  （原先采样点缓存在标注层里，检查器那条路径每次都要重新采样）
- 缓存容量固定（结果 256 条、圆 64 条，按写入顺序淘汰），量再久也不会无限涨；
  `TileKit` 不绑主线程，所以加了锁，标注图层与检查器从哪边取都安全
- 另开放 `evaluateUncached`（自检用来逐字段对照）与命中数／条目数（自检用来核对行为）
- 实测：自检里 200 点多边形重复求值 200 次，每帧重算 53.4 ms、缓存命中 1.0 ms（约 50 倍）；
  真机跑一遍缩放脚本（3 条示例测量，含一个圆），缓存命中 464 次
- 自检新增 18 项

### 功能：导出当前视图为图片（⌘⇧E / ⌘⇧C）

判读与量测的结果常常要进报告，之前只能自己截屏。现在：

- 画面就是画布本身（含瓦片、测量标注与读数），下方拼一条信息栏：
  数据源、叠加来源与版权、测量条数、中心坐标、层级，以及一条与屏幕同源的比例尺
- 保存面板里可以取消「包含测量标注」导出干净的地图；
  调试用的瓦片网格永远不进成图
- ⌘⇧C 直接把同一张图放进剪贴板
- 合成与 PNG 编码在 `ViewExporter`；画面本身仍由画布离屏渲染
  （`renderMapImage(includingMeasurements:)`，调试快照与出图共用这一条路径）
- 比例尺刻度抽成 `ScaleBarMetric`，屏幕上的浮动比例尺与成图里的那条用同一份算法

### 修复：离屏渲染出来的文字是上下颠倒的

出图时发现所有标注文字都是镜像的。原因是 `CALayer.render(in:)` 在 y 向下的上下文里
会把 `CATextLayer` 的文字画成上下镜像（瓦片那种「画图片」的内容不受影响），
屏幕上的合成路径则一直正常——所以这个毛病只在离屏渲染（导出图片、调试截图）里看得到。
现在出图前给文字图层补一次纵向翻转、出图后立刻还原
（`MeasurementOverlay.withLabelsUprightForOffscreenRender`）。

验证：本地数据集本身用只读 HTTP 服务当在线底图，`EUCLID_DEMO_MEASUREMENT=1` 铺上示例测量，
`EUCLID_DEBUG_EXPORT_VIEW` 写出成图——浅色与深色各一张，核对文字方向、信息栏内容
（`某影像数据集　测量 3 条`／`中心 116.397400 39.909300 · z13`）与右下角比例尺（2 km）。

## 质量保障

- `TileKitCheck` 共 287 项检查：投影往返、瓦片编号对照真实数据集、相机变换与锚点不变性、
  Vincenty 标准算例与直接/反向互逆、圆的周长与面积对照 `2πr` / `πr²`、样式编解码与回落、
  Excel 包的 ZIP 结构自校验、边界与异常输入、导出格式、真实数据集嗅探
- 坐标基准：互转可逆性、境外恒等、与公开实现逐位对照、
  世界坐标偏移与米制偏移一致、偏移基准下的下载计划编号
- 测量结果缓存：缓存与直算逐字段一致、同一条测量重复取用只算一次、顶点改动立刻重算、
  圆的度量与采样点一起命中、容量有上限且淘汰后仍算得对
- 构建产物经真机启动截图核对（瓦片渲染、测量标注、比例尺、检查器数据）

## 后续可做

- 量角器、多段面积累加、椭圆/矩形等更多测绘工具
- Excel 模板化导出（按项目自定义列）与已有测量结果的批量导入
- 出图加一个「带专题图元素」的模板（图廓、指北针、比例尺样式可调）
- 高精度配准：分块计算基准偏移（现在按视图中心做线性近似，省级视野下有几十米误差）
- 装上 Xcode 后可加 Metal 渲染后端与正式的开发者签名/公证
