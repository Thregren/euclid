# 尺规 · 技术路线

> 版本 v1 ｜ 2026-09-23 ｜ 状态：待确认后开工

## 0. 结论速览

| 维度 | 选型 |
| --- | --- |
| 语言 | Swift 6（严格并发），架构 arm64 |
| 界面 | SwiftUI 负责窗口/侧栏/工具栏/检查器，AppKit 负责地图画布 |
| 渲染 | Core Animation 图层金字塔（每瓦片一个 CALayer），GPU 合成 |
| 投影 | Web Mercator (EPSG:3857) ↔ WGS84，标准 slippy 公式 |
| 测距 | WGS84 椭球测地线（Vincenty 反向公式），分段累计 + 方位角 |
| 测面积 | 多边形测地面积（球面过剩 + 椭球修正） |
| 画圆 | 圆心 + 半径点两点表示，半径走 Vincenty 直接/反向公式，圆周 360 点测地采样 |
| 逐条样式 | 每条测量独立保存描边/填充颜色、描边粗细、填充不透明度（颜色覆盖值为可选） |
| 导出 | Excel（.xlsx，零依赖 OOXML + 存储式 ZIP）／GeoJSON／KML／CSV |
| 构建 | SwiftPM + 构建脚本打包 `.app`（本机无 Xcode，已验证可行） |
| 依赖 | 零第三方依赖（SQLite3 用系统库，为 MBTiles 预留） |
| 最低系统 | macOS 14 (Sonoma) |

## 1. 现状调研

### 1.1 已有瓦片数据

拿到的是多套 WebODM 正射影像瓦片集，实测特征如下：

| 维度 | 实测结果 |
| --- | --- |
| 目录结构 | `<根目录>/<z>/<x>/<y>.<jpg\|png>`，先列号目录、再行号文件 |
| 编号约定 | 标准 Google/XYZ（北起源），非 TMS 翻转 |
| 瓦片尺寸 | 512 × 512 |
| 图片格式 | 同一批数据里 JPEG 与 PNG 混用 |
| 层级范围 | 覆盖 z1–z22，各层级宽度不等 |
| 总规模 | 十几 GB、几十万个文件量级 |
| 覆盖形状 | 每个数据集都是稀疏覆盖，同一层级内并非矩形满铺 |

实测结论（直接决定解析器写法）：

1. 「先列号目录、再行号文件」是这批数据的实际编排，解析器不能假定 `<z>/<y>/<x>`；
   个别数据集在根目录下还多套了一层同名目录，需要向下探测。
2. 编号是标准 **Google/XYZ（北起源）** 值，不是 TMS 翻转值。
   用瓦片编号反算经纬度后与影像内容对得上，这一点在渲染阶段也得到验证。
3. **每个数据集都是稀疏覆盖**：同一层级内只有实际拍摄区域有图，其余是空洞，
   查看器必须优雅处理缺片。
4. 数据量意味着 **启动时绝不能全量扫描**。最高层级单层就有几十万个文件，
   一次 `ls` 级别遍历要数十秒，必须改成按需列举 + 负缓存。

### 1.2 本机开发环境

| 项 | 实测结果 | 影响 |
| --- | --- | --- |
| macOS | 26 系列，Apple Silicon | — |
| Swift | 6.2.3（CommandLineTools） | 可编译 Swift 6 语言模式 |
| Xcode | **未安装**（只有 CommandLineTools） | 无 `xcodebuild`、无 Interface Builder、无 Preview |
| SwiftPM | 可用（需 `--disable-sandbox`） | 已实测编译链接 SwiftUI + AppKit + QuartzCore + SQLite3 成功 |
| Metal 编译器 | **不存在**（`xcrun metal` 找不到） | 无法预编译着色器 |
| Metal 设备 | `MTLCreateSystemDefaultDevice()` 返回 nil | 运行时 Metal 不可用 |

构建与运行均已实测跑通：SwiftPM 编译出的图形程序可以正常启动并显示窗口。

> 结论：**走 Core Animation 图层渲染，不赌 Metal。** 需要 Xcode 的能力（Metal 着色器、SwiftUI Preview、正式签名）都列为可选增强，不阻塞主线开发。

## 2. 系统架构

```
EuclidApp (SwiftUI)                     TileKit (纯 Swift，可单测)
├── App / Commands / Settings              ├── Mercator         投影与瓦片数学
├── Sidebar 数据源与图层列表  ────────────►  ├── TileSource       数据源协议 + 适配器
├── Inspector 测量结果 / 坐标面板            │   ├── XYZFileSource   目录型（本项目两种布局）
├── Toolbar / 状态栏 / 比例尺                │   ├── MBTilesSource   SQLite 离线包（预留）
└── MapCanvasView (NSViewRepresentable)     │   └── AutoDetect     布局与层号自动嗅探
        └── TileCanvasView (AppKit)         ├── TileIndex        按需列举 + 负缓存 + 覆盖范围
            ├── TileLayerView  CALayer 金字塔 ├── TileProvider     actor，并发加载 + 优先级
            ├── OverlayView  测量标注层      ├── Geodesy         测地线距离 / 方位角 / 面积
            └── InputHandler  鼠标滚轮键盘    └── Datum            WGS84 / GCJ-02 / BD-09
```

分层原则：`TileKit` 不依赖 UI，全部算法在此层并可单元测试；UI 层只做状态与呈现。

## 3. 核心算法

### 3.1 投影与坐标换算

- WGS84 经纬度 ↔ Web Mercator 米（正/反算，标准 slippy 公式）
- 经纬度 ↔ 瓦片 `(z, x, y)` ↔ 瓦片内像素 `(px, py)` 三向互转，全程 `Double`，往返误差 < 1e-9
- 视图变换：视口 ↔ 墨卡托的仿射矩阵（缩放 + 平移），所有命中测试走同一套矩阵，保证像素级对齐
- 派生信息：比例尺（1 px 对应实地米数）、当前分辨率、可见瓦片集合

### 3.2 距离测量

- 分段距离用 **Vincenty 反向公式**（WGS84 椭球，毫米级收敛），纯 Swift 实现约 60 行
- 每条边同时给出：长度、正/反方位角、与上一条边的转角
- 总长度、起点到终点直线距离、闭合差
- 拖动预览阶段用**球面 haversine** 快速估算（每帧都要算，省 CPU），松手后切回精密算法

### 3.3 面积与坐标读数

- 多边形面积用测地面积公式（球面过剩 + 纬度带修正），不自建墨卡托平面近似，避免高纬度误差
- 坐标读数同时显示：十进制度、度分秒、Web Mercator 米、当前 `z/x/y` 与瓦片内像素

### 3.4 坐标系基准（关键风险点）

国内常见瓦片来源与基准的对应关系：

| 来源 | 基准 | 与 WGS84 的偏差 |
| --- | --- | --- |
| 天地图 / 自采卫星影像 | CGCS2000 ≈ WGS84 | 可忽略 |
| 高德 / 腾讯 | GCJ-02（火星坐标） | 城区 100–500 m |
| 百度 | BD-09 | 更大 |

若这两个数据集的底图来自高德系，直接按 WGS84 读数会出现几百米偏移，这对测量工具是硬伤。因此设计里把 **基准作为数据集的属性**，提供 WGS84 / GCJ-02 / BD-09 三档切换与互转算法，界面上明确标注当前基准。首次打开数据集时可让用户确认一次，之后记住。

> 已按这条落地（见 `TileKit/Datum.swift` 与进度 M11）：基准现在是**数据源的属性**，
> 在线底图按视图中心处的偏移整体平移瓦片，与 WGS84 的正射影像叠加对齐；
> 坐标读数、测量与导出始终走 WGS84 工作基准。
> 偏移取的是线性近似——城区（几公里）内误差在米级，省级视野下会差几十米，
> 更高精度需要按瓦片分块换算（列在「后续可做」里）。

### 3.5 单幅影像直读（GeoTIFF）

瓦片目录要求先把正射影像切好，而 ODM / WebODM 的原生产物就是**一整幅**
`odm_orthophoto.tif`（常见形态：分块 256 × 256 + Deflate + Predictor 2 + RGBA，带内建概览，
投影多为 UTM 或经纬度）。因此「打开就能看」这条路径单独做一层：

- **目录结构自己读**：`TIFF.swift` 解析 IFD（含 BigTIFF、IFD 链与 SubIFD），
  拿到尺寸、条带/分块表与 GeoTIFF 标签；像素一律**按需解**，内存与文件大小无关
- **压缩自己解**：`TIFFDecoder.swift` 覆盖未压缩 / Deflate / LZW / PackBits / JPEG，
  8 与 16 位、灰度 / RGB / 调色板 / alpha、Predictor 2。
  注意系统 `Compression` 框架的 `COMPRESSION_ZLIB` 实际只接受**裸 deflate**，
  而 TIFF 里是 RFC 1950 包装，必须先剥外壳
- **定位换算**：`Projection.swift` 提供经纬度 / Web 墨卡托 / 横轴墨卡托
  （UTM、CGCS2000 三度与六度带、西安 80、北京 54、文件内自定义参数）到 WGS84 的换算；
  认不出坐标系时按「未配准」显示，绝不猜一个位置
- **接入渲染**：`RasterTileSource` 把瓦片请求换算成影像像素区域，采样成瓦片画布，
  于是单幅影像与瓦片数据集在下游完全同构（同一套缓存、测量、出图）

## 4. 渲染与性能

### 4.1 渲染方案

每个可见瓦片对应一个 `CALayer`，`contents` 直接指向解码后的 `CGImage`，位置由统一的仿射变换决定。平移缩放时只改图层 transform，由窗口服务器在 GPU 上合成，**CPU 不参与重绘**。这是 MapKit 与 `CATiledLayer` 的同款思路，在本项目规模下性能充裕。

瓦片在整数层级上取用最接近的一层，跨层时用 `magnificationFilter = .trilinear` 平滑放大；`contentsScale` 跟随屏幕倍率，Retina 上文字与线划不糊。

测量标注层独立于瓦片层，用 `CAShapeLayer` + `CATextLayer` 绘制折线、顶点、边长标签。这样平移时瓦片不动、标注跟着动，互不干扰。

> 后续若装上 Xcode，可在 `TileCanvasView` 后加 Metal 后端做重采样与锐化，不影响现有结构。

### 4.2 数据加载

- **按需列举**：只为正在查看的层级枚举目录，结果缓存；缺失瓦片记入负缓存，避免反复 IO
- **两级缓存**：`NSCache` 按字节数限制内存（默认 512 MB 可调），可选磁盘缓存存解码结果
- **并发**：`actor TileProvider` 管理加载队列，`TaskGroup` 并行解码，优先级为「当前视口 > 视口外一圈预取 > 远景」
- **取消**：视口快速变化时取消已无意义的加载任务，避免抖动
- **索引**：首次打开数据集时后台生成层级索引（各层 bbox、瓦片数、空片比例），用于「缩放到数据范围」与层级切换；UI 不阻塞，进度可显示

### 4.3 目标指标

| 指标 | 目标 |
| --- | --- |
| 平移/缩放帧率 | 60 fps（Apple Silicon） |
| 首次出图 | 打开数据集 < 1.5 s |
| 瓦片命中 | 内存命中 < 1 ms，磁盘命中 < 10 ms |
| 坐标读数延迟 | < 16 ms（跟随鼠标无卡顿） |
| 大目录安全 | z22 级别（41 万片）目录打开不冻结 UI |

## 5. 交互与界面（苹果原生观感）

### 5.1 窗口结构

```
┌─────────────┬───────────────────────────────────┬─────────────┐
│ 侧栏         │            地图画布                │ 检查器       │
│             │                                   │             │
│ 数据源        │      [瓦片 + 测量标注层]            │ 坐标读数      │
│  ├ 数据集 A   │                                   │ 测量结果      │
│  └ 数据集 B   │  左下：比例尺  右下：层级/分辨率      │ 导出         │
│ 图层可见性    │                                   │             │
│ 书签/标注     │                                   │             │
└─────────────┴───────────────────────────────────┴─────────────┘
              底部状态栏：坐标 · z/x/y · 像素 · 基准
```

- `NavigationSplitView` 三段式，侧栏与检查器可折叠（⌘⌥S / ⌘⌥I）
- 工具栏用 SF Symbols，分组排列，常用动作给快捷键
- 悬浮控件用 `.ultraThinMaterial` 毛玻璃，圆角与系统一致
- 自动深/浅色，语义色（`Color.accentColor`、`labelColor`），不写死颜色
- 菜单栏齐备：文件（打开/最近）、视图（缩放/层级/适配）、测量、窗口、帮助

### 5.2 工具与快捷键

| 工具 | 操作 | 快捷键 |
| --- | --- | --- |
| 浏览 | 鼠标滚轮/捏合缩放、中键拖动或左键拖动或用双指滚动平移、双击放大、⌥双击缩小 | V |
| 点坐标 | 点击取点，读数进检查器，可复制 | C |
| 测距 | 点击加点成折线，双击/回车结束，Esc 取消，⌫ 删点，拖动顶点微调 | D |
| 测面积 | 点击加点成多边形，回车闭合，显示周长与面积 | A |
| 画圆 | 点击定圆心，再点一次定半径，拖动半径点或输入半径（米/公里）微调 | O |
| 适配窗口 | 缩放到数据范围 | ⌘0 |
| 打开数据集 | 选目录 | ⌘O |

吸附与约束：顶点吸附、边中点吸附、按住 Shift 约束水平/垂直/45°，按住 ⌘ 临时禁用吸附。

### 5.3 导出

测量结果可导出 Excel（`.xlsx`）、GeoJSON / KML / CSV，也可一键复制当前读数或整表到剪贴板。

Excel 走零依赖自研路径：OOXML 表格由 `XLSX.swift` 拼 XML 部件，再用同一个文件里的
存储式 ZIP 打包器（含 CRC32）装成 `.xlsx`。OOXML 允许成员不压缩，因此不必引入 deflate。
四张工作表分别是测量汇总、点坐标（含 Web Mercator 东/北坐标）、分段明细与说明；
自检里用配套的 `ZIP.parse` 反解并逐条校验 CRC，确保产物是真能被 Excel 打开的包。

每条测量的描边颜色、填充颜色与填充不透明度都随测量保存；未指定颜色时继续用调色板语义色，
因此默认外观仍然跟随系统深浅色切换。

## 6. 工程结构与构建

```
瓦片浏览器/
├── Package.swift
├── Sources/
│   ├── TileKit/            # 核心库（无 UI 依赖，可单测）
│   └── EuclidApp/      # 可执行 app
├── Tests/TileKitTests/
├── Scripts/build-app.sh    # 编译 + 组装 .app + ad-hoc 签名
├── Resources/AppIcon.icns
├── docs/
└── 瓦片/                    # 数据（.gitignore 排除）
```

构建脚本流程：`swift build -c release --disable-sandbox` → 组装 `TileViewer.app/Contents/{MacOS,Resources}` → 写 `Info.plist`（含 `LSMinimumSystemVersion`、`NSHumanReadableCopyright`）→ `codesign -s -` ad-hoc 签名 → 可选生成 DMG。

这套流程本机已验证可行，产出的是标准 `.app`，双击即可运行，也能拖进「应用程序」。

## 7. 测试与验收

- 单元测试：投影往返一致性、瓦片定位、Vincenty 与已知测例对照（例如福州—厦门基线，误差 < 1 mm/km）、面积公式对照、GCJ-02 互转可逆性
- 数据测试：对两个真实数据集验证布局嗅探、层级索引、缺片处理
- 视觉验收：构建后实际启动截图，逐项核对界面；重大改动都截图确认

## 8. 里程碑

| 阶段 | 内容 | 交付物 |
| --- | --- | --- |
| M1 | 工程骨架 + 瓦片解析 + 图层渲染 + 平移缩放 | 能打开目录、看到地图并自由拖动 |
| M2 | 坐标换算 + 层级切换 + 状态栏 + 比例尺 + 数据范围适配 | 鼠标位置实时坐标，可缩放层级 |
| M3 | 测距工具（折线、分段、总长、方位角、吸附） | 能量尺寸并出结果 |
| M4 | 测面积 + 点坐标 + 检查器面板 + 导出 | 完整测量能力 |
| M5 | 界面精修、缓存调优、多数据源、书签、打包 .app | 可日常使用的成品 |
| M6 | 画圆、逐条样式（描边/填充颜色与不透明度）、Excel 导出、浏览手势收敛 | 出图与出数据都够用 |

每个里程碑结束都会构建并截图给你确认，再进入下一阶段。

## 9. 风险与待确认

1. **坐标系基准**：这两个数据集的底图来源是天地图系还是高德系？直接决定坐标读数是准的还是偏几百米。（默认按 WGS84，界面可切换）
2. **精度要求**：测距是够用就行（米级），还是需要测绘级（结合地方坐标/投影带）？
3. **是否需要 MBTiles / 离线包**：当前是散文件目录。如果以后要支持 `.mbtiles` 单文件，架构已预留，但需要额外开发。
4. **是否要装 Xcode**：不装也能完整开发和交付 `.app`；装了可以做 SwiftUI Preview、Metal 后端和正式签名公证。目前按「不装」推进。
