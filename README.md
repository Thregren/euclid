# 尺规 · Euclid

macOS 原生本地瓦片查看器，为 **WebODM / ODM 输出的正射影像 XYZ 瓦片**设计，
支持坐标读取、折线测距、多边形测面积与画圆。没有联网依赖，所有计算在本机完成。

当前版本 **1.7.0**，下载见 [Releases](https://github.com/Thregren/euclid/releases/latest)。

界面为三栏结构：左侧是数据源与最近打开，中间是瓦片画布（左下角浮动缩放控件与比例尺），
右侧检查器实时显示指针坐标、测量结果与数据集信息，底部是状态栏。

## 特性

- **流畅浏览**：Core Animation 图层金字塔渲染，平移缩放由窗口服务器在 GPU 上合成，
  拖动时不重绘 CPU；稀疏覆盖、缺片、符号链接挂载都能正确处理
- **坐标读取**：度分秒、十进制度、Web Mercator 米、瓦片 `z/x/y`、瓦片内像素，
  一键复制或输入经纬度跳转
- **测距**：多点折线，逐段给出长度、方位角、罗盘方位与转角，另有总长与起终点直线距离
- **测面积**：多边形测地面积、周长与闭合边长
- **画圆**：点一下圆心、再点一下定半径，之后拖动半径点或输入精确半径（米 / 公里）
- **逐条样式**：每条测量可单独设置描边颜色、描边粗细、填充颜色与填充不透明度
- **测量辅助**：顶点吸附、拖动微调、Shift 约束正交与 45°、多条测量并存且各自配色
- **导出**：Excel（`.xlsx`，汇总／点坐标／分段明细／说明四张表）、GeoJSON / KML / CSV，或直接复制到剪贴板
- **在线瓦片下载**：按当前视图或手输经纬度范围，选层级区间，把在线瓦片源批量取到本地目录
  （落成 `<z>/<x>/<y>`，下载完直接打开即可离线浏览）；内置天地图、OpenStreetMap 等预设，
  也可填任意 URL 模板（`{z}` `{x}` `{y}` `{-y}` `{s}` `{key}`），带并发与限速控制
- **在线底图浏览**：侧栏或工具栏一键开关在线瓦片源，取图走同一套内存缓存与并发闸门，
  平移时还会预取视野外一圈；测量、坐标读取在在线底图上照常可用
- **叠加对照**：本地影像与在线底图可以同时显示，各自带不透明度滑杆
  （把影像调到 55% 就能透过正射影像看底图路网，用来核对配准）
- **坐标基准对齐**：底图来自高德 / 腾讯（GCJ-02）或百度（BD-09）时，在侧栏把「坐标基准」
  切一下，在线底图就按该基准的偏移与 WGS84 正射影像对齐；偏移的方向与量级会写在状态栏与检查器里
- **出图**：⌘⇧E 把当前视图存成 PNG（画面下方带一条信息栏：数据源、中心坐标、层级与比例尺），
  ⌘⇧C 直接复制到剪贴板；出图包含测量标注与读数，贴进报告就能看懂
- **原生体验**：三栏结构、检查器、工具栏、状态栏、比例尺、深浅色自适应

## 快速开始

不想编译就直接下 [Releases](https://github.com/Thregren/euclid/releases/latest) 里的
`Euclid-<版本>-macos-arm64.zip`（Apple Silicon，macOS 14+），解压后把「尺规.app」拖进「应用程序」；
发布包是 ad-hoc 签名、未做公证，首次打开需要右键「打开」。

从源码构建：

```bash
git clone https://github.com/Thregren/euclid.git
cd euclid
./Scripts/build-app.sh
open "dist/尺规.app"
```

需要 macOS 14+ 与 Swift 6 工具链（Xcode 或 CommandLineTools 均可）。
**没有 Xcode 也能完整构建**，脚本会直接产出可双击的 `.app`。

自检（可选带数据集目录）：

```bash
./Scripts/run-checks.sh
./Scripts/run-checks.sh /path/to/tiles
```

## 使用

打开数据集：⌘O 选择文件夹，或把文件夹拖到窗口 / Dock 图标上；应用会记住上次打开的目录。
也可以直接选择装数据集的父目录，应用会向下找三层。

| 工具 | 快捷键 | 操作 |
| --- | --- | --- |
| 浏览 | V / ⌘1 | 鼠标滚轮或捏合缩放，中键拖动、左键拖动、双指滚动平移，双击放大，⌥双击缩小 |
| 点坐标 | C / ⌘2 | 单击取点，坐标显示在检查器 |
| 测距 | D / ⌘3 | 点击加点，双击或回车结束，⌫ 撤销，Esc 取消 |
| 测面积 | A / ⌘4 | 点击加点，双击或回车闭合 |
| 画圆 | O / ⌘5 | 点击定圆心，再点一次定半径；之后可拖动半径点或输入半径 |

测量时按住 Shift 约束方向为水平、垂直或 45°，按住 ⌘ 关闭顶点吸附，
拖动任意顶点可微调已完成的测量。⌘0 适配数据范围，⌘⇧0 恢复原始比例。
选中某条测量后，检查器里可以单独调整它的描边颜色、描边粗细、填充颜色与填充不透明度
（「应用到全部」可一键统一）。

出图：⌘⇧E 打开保存面板把当前视图存成 PNG，⌘⇧C 直接复制到剪贴板。
成图就是画布本身（含测量标注与读数），下面多一条信息栏写明数据源、中心坐标、层级与比例尺；
保存面板里可以取消「包含测量标注」，导出一张干净的地图。

## 架构

### 总览

```
┌─────────────────────────── EuclidApp（可执行，UI 层）───────────────────────────┐
│  SwiftUI 外壳                        AppKit 画布                               │
│  ├ RootView / SidebarView             └ TileCanvasNSView                      │
│  ├ InspectorView + InspectorSections       ├ tileHostLayer  瓦片图层金字塔      │
│  ├ StatusBarView / ScaleBarView            ├ gridLayer      瓦片网格            │
│  └ MapControls                             └ MeasurementOverlay  测量标注      │
│                                                                               │
│  状态：AppModel（应用状态）· ViewportState（视图读数）· MeasurementStore（测量）│
└───────────────────────────────────┬───────────────────────────────────────────┘
                                    │ 单向依赖
┌───────────────────────────────────▼───────────────────────────────────────────┐
│                        TileKit（核心库，无 UI 依赖，可单测）                     │
│  GeoCoordinate / WebMercator   投影与坐标换算                                   │
│  SlippyTile / TileLayout       瓦片标识与磁盘布局                               │
│  DatasetDiscovery              数据集嗅探、布局判定、覆盖范围                    │
│  DirectoryTileSource           散文件数据源读取                                 │
│  TileProvider                  actor：LRU 缓存 + 并发解码闸门 + 缺片负缓存      │
│  MapCamera                     视图变换、缩放锚点、可见瓦片范围                  │
│  Geodesy / Measurement         测地线距离、面积、测量模型                        │
│  MeasurementExport             GeoJSON / KML / CSV / Excel(.xlsx)               │
│  XLSX                          零依赖 OOXML 生成 + 存储式 ZIP 打包              │
└───────────────────────────────────────────────────────────────────────────────┘
                                    ▲
                    TileKitCheck ───┘  自带断言的自检程序（287 项）
```

依赖方向严格单向：`TileKit` 不认识 UI，`EuclidApp` 只消费 `TileKit`，
因此所有投影、测量、解析逻辑都可以脱离界面测试。

### 目录结构

```
Sources/TileKit/          核心库
  GeoCoordinate.swift       坐标类型、Web Mercator 正反算
  SlippyTile.swift          瓦片标识、目录布局与行号约定
  DirectoryTileSource.swift 目录型数据源、图片解码
  DatasetDiscovery.swift    数据集嗅探、扩展名与布局判定、覆盖范围统计
  TileProvider.swift        图片供应者（LRU 缓存、并发闸门、负缓存）
  MapCamera.swift           相机变换与可见瓦片计算
  Geodesy.swift             Vincenty 测地线、面积、显示格式化
  Measurement.swift         测量模型与结果计算
  MeasurementStyle.swift    与 UI 无关的颜色分量与逐条样式
  MeasurementExport.swift   导出格式（含 Excel 四张表）
  XLSX.swift                极简 OOXML 表格生成 + 存储式 ZIP 打包与校验
Sources/EuclidApp/        应用层
  EuclidApp.swift           @main 入口、AppDelegate、菜单命令
  AppModel.swift            应用状态、数据集打开流程、导出动作
  RootView.swift            窗口结构、工具栏、工具提示
  SidebarView.swift         数据源列表、位置、最近打开
  InspectorView.swift       检查器整体布局
  InspectorSections.swift   指针坐标分区、测量结果分区
  TileCanvasNSView.swift    AppKit 画布：瓦片渲染、缩放平移、工具交互
  TileMapView.swift         NSViewRepresentable 桥接
  MeasurementOverlay.swift  测量标注图层渲染
  MeasurementStore.swift    工具状态机、草稿与已完成测量
  StatusBarView.swift       底部状态栏
  ScaleBarView.swift        比例尺
  CoordinateText.swift      坐标文本格式化
  DebugFixtures.swift       调试示例数据（环境变量开启）
Sources/TileKitCheck/     自检程序
Scripts/                  构建与自检脚本
```

### 关键设计决策

**渲染：CALayer 金字塔，不用 Metal。**
每个可见瓦片对应一个 `CALayer`，`contents` 直接指向解码后的 `CGImage`。
平移缩放只改图层 transform，由窗口服务器合成，CPU 不参与重绘——
与 MapKit、`CATiledLayer` 同思路。选它而不是 Metal 的原因是本机环境没有 Metal 着色器编译器
（`xcrun metal` 不存在），且图元简单、不需要自定义着色器；
渲染路径封装在 `TileCanvasNSView` 内，将来要换后端不影响其它层。

**数据：按需列举，绝不全量扫描。**
单个数据集可能有几十万个瓦片文件，全量遍历要几十秒。
应用只为当前层级枚举目录，缺片记入负缓存避免重复 IO；
覆盖范围统计会挑一个规模适中的层级取样（默认目录数 ≤ 400），
百万级文件的数据集也能秒开。

**布局嗅探：用「另一种编排是否存在」判定轴向。**
`<z>/<a>/<b>` 里 a 是列号还是行号，靠检查磁盘上是否存在 `<z>/<b>/<a>` 来判断——
因为只有正确的轴向才会存在转置路径。行号基准（XYZ 北起源 / TMS 南起源）无法从文件名推断，
按 WebODM 默认约定取 XYZ，并在数据结构里保留可配置项。

**坐标约定：三种坐标系各司其职。**
世界坐标是归一化的 Web Mercator（`0...1`，x 向东、y 向南）；
NSView 未翻转，视图坐标 y 向上，鼠标事件直接可用；
图层坐标 y 向下，靠 `isGeometryFlipped` 统一，瓦片与测量标注共用同一套换算。
所有换算围绕 `MapCamera` 一处收敛，避免多套变换互相打架。

**并发：UI 全在主线程，解码交给 actor。**
`TileProvider` 是 actor，内部用 LRU 缓存（按字节限流）、
固定并发闸门（`AsyncLimiter`，默认同时 6 个解码）与缺片负缓存；
`Task.detached` 负责实际的磁盘读取与解码，结果回到主线程装进图层。
视口变化时按「离视图中心近的优先」排序请求，数据集切换用世代号丢弃过期结果。

**测量：几何计算与交互分离。**
`Geodesy` 只做数学（Vincenty 反向公式、球面过剩面积），`MeasurementStore` 管状态机
（草稿、完成、选中、撤销），`MeasurementOverlay` 只管画。
距离一律走 WGS84 椭球公式而非墨卡托平面距离，避免纬度带来的系统误差；
拖动预览阶段才用轻量近似，保证每帧不掉帧。
求值结果按「测量类型 + 顶点序列」记忆（圆另按「圆心 + 半径」记忆，连 360 点采样一起），
于是平移缩放时每一帧只是取缓存、按新的相机换算屏幕位置，不会重算测地线。

**坐标系基准：数据源的属性，不是全局开关。**
WebODM / ODM 输出的正射影像基于 WGS84（CGCS2000 与 WGS84 的差异在厘米级），
而国内不少底图用的是 GCJ-02（高德、腾讯，城区偏移 100–500 m）或 BD-09（百度）。
于是基准记在数据源上：`Datum` 提供 WGS84 / GCJ-02 / BD-09 三档互转
（正向是公开的多项式拟合，反向迭代逼近，往返误差 < 1 cm），
在线底图按「视图中心处的偏移」整体平移瓦片，与 WGS84 的正射影像一叠就能对上；
坐标读数、测量与导出始终走 WGS84 工作基准，不受底图基准影响。
偏移取的是视图中心处的线性近似：一片城区（几公里）内误差在米级，省级视野下会差几十米 ——
用来判读与配准检查够用，但不要拿它当高精度配准。

### 扩展点

- **加数据源**（MBTiles、PMTiles、WMS）：实现与 `DirectoryTileSource` 等价的小接口，
  在 `TileProvider` 处替换即可，渲染与测量层不用改
- **加测量工具**（量角器、多段面积累加）：在 `MeasurementKind` 加枚举分支、
  `MeasurementCalculator` 补计算、`MeasurementStore` 补交互状态
- **加导出格式**：在 `MeasurementExporter` 增加一个静态方法，
  在 `MeasurementExportFormat` 登记即可出现在菜单里
- **加测量工具**：在 `MeasurementKind` 与 `MapTool` 各加一个分支，
  补上 `MeasurementCalculator` 的计算与 `MeasurementOverlay` 的绘制即可

## 质量保障

`./Scripts/run-checks.sh` 覆盖 287 项检查：

- 投影与瓦片编号往返一致性，并与真实数据集实测编号对照
- 相机变换互逆性、缩放锚点不变性、适配范围后的完整性
- **Vincenty 1975 标准算例**（Flinders Peak → Buninyong，与文献值相差 < 1 cm）
- Vincenty 直接公式与反向公式互逆（画圆与「输入半径」的基础）
- 测地圆的周长/面积与 `2πr` / `πr²` 对照、圆周采样点数、零半径边界
- 测地面积与独立解析值对照、面积与绕行方向无关
- 旧存档（缺 `style` 字段）解码回落、自定义样式编解码往返、样式取值范围收敛
- Excel 导出：ZIP 结构自校验（逐条 CRC）、部件清单、各表行数与关键内容
- 相机在缩放上下限处连续缩放不产生位移、锚点补偿不被层级夹取破坏
- 在线下载：范围到瓦片行列的换算（含边界对齐不多取一圈）、模板占位符渲染与密钥处理、
  缺片/重试/跳过已存在、并发上限、进度单调、取消、落盘清单与「下载目录可被识别为数据集」
- 在线取图：远程来源的层级夹取、失败重试、404 视为缺片、缺密钥判定，
  以及供应者的解码、内存缓存命中、缺片负缓存、预取与缓存失效
- 坐标基准：境外不做偏移、境内偏移量级在百米级、一键往返误差在厘米级、
  与公开流传的实现逐位对照（WGS84 → GCJ-02）、BD-09 等于 GCJ-02 再偏移，
  以及「偏移基准下的下载计划要按该基准的瓦片网格取编号」
- 测量结果缓存：缓存路径与直算逐字段一致、同一条测量重复取用只算一次、
  顶点改动后立刻重算、圆的度量与采样点一起命中、容量有上限且淘汰后仍算得对
- 边界与异常输入：同点、近对跖点、越界瓦片、少于三点的多边形、方位角归一化
- 导出格式结构校验、真实数据集嗅探（布局、层级、瓦片尺寸、覆盖范围）

## 已知限制

- 基准偏移取的是**视图中心处**的线性近似：一片城区（几公里）内误差在米级，
  省级视野下会差几十米；用来判读与配准检查够用，不要拿它当高精度配准
- 首次打开「文稿」「桌面」「下载」中的目录时，macOS 会要求一次文件访问授权
- 测量结果按数据集目录自动存档到 `~/Library/Application Support/Euclid/measurements.json`，
  再次打开同一数据集会自动恢复；跨机器使用请先导出
- 发布包为 ad-hoc 签名，其它机器首次打开需要右键「打开」，或执行
  `xattr -dr com.apple.quarantine /Applications/尺规.app`

## 文档

- [技术路线](docs/01-技术路线.md)｜[构建与运行](docs/02-构建与运行.md)｜[开发进度](docs/03-进度.md)｜[使用说明](docs/04-使用说明.md)

## 更新日志

### 1.7.0

- **新增：导出当前视图为图片**（⌘⇧E）。成图就是画布本身（瓦片、测量标注与读数都在），
  下面多一条信息栏写明数据源、中心坐标、层级与比例尺，直接贴进报告或聊天窗口就能看懂；
  保存面板里可以取消「包含测量标注」导出一张干净的地图
- **新增：复制当前视图到剪贴板**（⌘⇧C），省掉「先存盘再拖进去」那一步
- **性能：测量结果不再每帧重算**。标注图层与检查器在平移、缩放、重绘时会反复取同一条测量的结果，
  而一次求值要走几十次 Vincenty（圆更狠：360 点采样 + 周长 + 面积，上千次测地运算）。
  现在按「类型 + 顶点」缓存结果、圆另按「圆心 + 半径」缓存（连采样点一起），
  容量固定不会涨：自检里 200 点多边形重复求值 200 次，重算 53 ms、命中 1.0 ms，约 50 倍；
  真机跑一遍缩放脚本，命中 464 次
- **修复：导出的图片里文字是上下颠倒的**。`CALayer.render(in:)` 在 y 向下的上下文里会把
  `CATextLayer` 的文字画成镜像（瓦片这类图像内容不受影响），出图前给文字图层补一次纵向翻转即可；
  屏幕上的显示路径一直是对的，之前只在离屏渲染（导出图片、调试截图）里看得出来
- **修复：白底影像上每隔一片有一条浅灰细线**。缩放比例不是整数倍时，图层边缘会带出半像素的
  采样渗色，一整片白底（WebODM 输出的瓦片常带不透明白边）上就看成一格一格的细线；
  现在每格向外撑开半个点，让相邻瓦片互相盖住这道缝
- 比例尺刻度抽成屏幕与出图共用的一份算法，图上的比例尺与界面里看到的不会两样
- 自检扩到 287 项（新增测量结果缓存的一致性、命中、淘汰与耗时对照）
- 顺带修掉一个调试隐患：`EUCLID_DEMO_MEASUREMENT` 铺出来的示例测量会写进存档、
  覆盖用户自己量的结果（做真机截图核对的副作用），现在这条路径不再落盘

### 1.6.0

- **坐标基准对齐（GCJ-02 / BD-09）**：国内不少底图并不在 WGS84 上——高德 / 腾讯用 GCJ-02
  （城区偏移 100–500 m），百度用 BD-09。侧栏「底图」里现在可以选「坐标基准」，
  在线底图会按该基准的偏移整体平移瓦片，与 WGS84 的正射影像叠加时能对齐；
  状态栏与检查器里写明偏移方向与量级（例：`GCJ-02 相对 WGS84 向东 492 m、向北 360 m`），
  基准选错一眼就能看出来
- 互转用公开的多项式拟合（正反向往返误差 < 1 cm），境外坐标不做偏移，与各家实现一致
- **下载也认基准**：下载面板共用同一个基准设置，按该基准的瓦片网格取编号，
  `source.json` 与来源说明里会记下基准
- 修复：只改基准（模板与密钥都没动）时在线图层不会重装，画面仍按旧偏移显示

### 1.5.0

- **叠加对照**：本地影像与在线底图不再互斥，可以同时显示，各带一条不透明度滑杆
  （侧栏「图层」里调，检查器里能看到当前百分比）——把影像调到 55% 就能透过正射影像看底图路网
- **画布重构**：瓦片图层的整套逻辑（层级切换兜底、祖先贴图、缺片负缓存、预取、网格）
  抽成可复用的 `TileLayerStack`，画布同时挂「在线底图（下）/ 本地影像（上）」两条，
  共用同一个相机，因此叠加时缩放平移与层级切换天然同步；画布文件从 1289 行降到 780 行
- 每层各自按自己的瓦片像素密度选层级：在线底图仍按地图约定，本地影像仍按设备像素 1:1，
  混用时不会互相带偏
- 自检修掉了「没有任何图层时缩放区间为空」的启动崩溃

### 1.4.0

按 Apple 人机界面指南（HIG）打磨了整个界面，并支持 macOS 26 起的 Liquid Glass：

- **排版与对比度**：全部改用系统语义字号（跟随系统文本大小，不再写死 11pt），
  小字字号不再使用三级灰；深浅色两种外观都逐屏核对过
- **控制层材质**：浮在地图上的缩放控件、比例尺与工具提示，在 macOS 26 及以上采用
  Liquid Glass（`.glassEffect`），更早系统退回标准材质加描边，层级关系一致
- **工具栏**：从 11 项精简到 8 项、只用符号并补全提示；「清除测量」并入测量菜单，
  「瓦片网格」并入底图菜单；窗口标题改为「当前在看什么」，不再拿 App 名当标题
- **侧栏**：「打开瓦片目录」入口从侧栏底部移到「数据源」标题旁（HIG：关键操作别放边栏底部）
- **加载态**：扫描目录与读取数据范围时，画布上显示转圈与说明，不再只留空白
- **无障碍与动效**：画布有可读的无障碍名称与当前层级；开启「减少动态效果」时
  跳过瓦片淡入与背景淡出；浮层按钮补上无障碍标签
- 下载面板的区块改用系统分组框（GroupBox），深浅色/增强对比度/降低透明度全交给系统

### 1.3.0

- **在线底图浏览**：侧栏或工具栏一键在「本地数据」与在线瓦片源之间切换，
  底图与下载共用同一套源参数（预设、URL 模板、密钥），两处不会各选各的
  - 取图走与本地数据集完全相同的一条渲染路径：同一套内存缓存、并发闸门与缺片处理，
    因此测量、坐标读取、比例尺在在线底图上照常可用
  - 在线底图按通用地图约定显示（一张 256px 瓦片铺满它自身的像素数），
    在 Retina 与普通屏上视觉比例尺一致；本地影像仍按设备像素 1:1，保证判读清晰
  - 视野安定后会预取外面一圈瓦片（本地走磁盘、在线走网络，单次最多 24 片），平移更顺
- **下载面板**：新增「复制范围 / 用剪贴板范围」和「复制瓦片清单（z,x,y）」，方便与 GIS 工具对接
- **代码整理**：取图通道抽象为 `TileImageSource` 协议，本地目录与在线服务各实现一份，
  渲染层不再区分数据来自磁盘还是网络；状态栏合并成同一套读数布局；
  自检扩到 232 项（新增在线取图、缓存命中/负缓存/预取/失效等 20 项）

### 1.2.0

- **在线瓦片下载**：工具栏「下载在线瓦片」（⌘⇧D）打开面板，按当前视图 / 数据范围 / 手输经纬度
  圈定区域，选层级区间，把在线瓦片取到本地目录，落成 `<z>/<x>/<y>` 布局，下载完可以直接打开离线浏览
  - 内置预设：天地图影像 / 影像注记 / 矢量（官方 WMTS，需要开发者密钥）、OpenStreetMap，
    另有「自定义模板」可填任意 XYZ 或 WMTS-KVP 模板（`{z}` `{x}` `{y}` `{-y}` `{s}` `{key}`）
  - 并发与限速可调，404 记为缺片不重试，5xx / 超时按指数退避重试，已存在的瓦片默认跳过，可随时停止
  - 输出目录里会写 `source.json` 与 `attribution.txt`，记录来源、范围、层级与使用条款
- **修复：加载与缩放时画面一闪一闪**（前一版未发布的 1.1.2 内容）
  - 换层级时新图层会先刷一层 35% 透明的占位色、而且盖在保留的上一层影像之上，
    于是每跨一次层级整屏先蒙一层灰白纱再恢复；现在新图层保持透明，由上一层影像顶着，
    自己的图到了再淡入
  - 首次落图原先直接硬贴，几十块瓦片按解码顺序一块块跳出来；现在首次出现也有 120 ms 淡入
  - 屏上只要有一格「自己没有图、祖先也没有图」，兜底逻辑就会一轮轮重来，
    打开数据集时每秒空转两百多轮全量重绘（同一帧日志实测刷了 2400 行）；现在把这类格子记下来，回路几轮内收敛
  - 数据范围算出来之前不再铺图：此前会停在数据集之外铺一屏空占位并白读磁盘，
    范围一到画面整体跳一次；这期间状态栏显示「正在读取数据范围…」

### 1.1.1

- **修复：切换缩放层级后瓦片整屏消失**。渲染层以前记着一个「请求过就不再请求」的集合，
  而换层级会把图层整批移除重建；重建后的图层因此永远不会再取图，只剩下空占位。
  现在按「这一帧还缺图、没有在途请求、也不是已确认缺片」来判断，回到访问过的层级也能正常显示
- **修复：缩放到上下限后继续滚动会平移视图**。原先先按未夹取的层级算锚点补偿、事后只修正比例尺，
  于是到了缩放极限后每一格滚动都变成一次平移，几下就把影像推出画面；
  现在先夹取目标层级再算补偿，到极限后继续滚动不再改变视图
- 取图队列会在有额度空出时自动补跑，超出并发上限的瓦片不必等下次交互才加载
- 自检新增 13 项：缩放上下限处连续缩放不应位移、锚点补偿不被夹取破坏

### 1.1.0

- **画圆**：新工具（O / ⌘5），点圆心、再点一次定半径，之后拖动半径点或输入精确半径（米 / 公里）；
  半径走 WGS84 测地距离，圆周按 360 点测地采样，周长与面积与画出的图形完全一致
- **逐条样式**：每条测量可单独设置描边颜色、描边粗细、填充颜色与填充不透明度，可一键应用到全部
- **Excel 导出**：零依赖生成 `.xlsx`，含测量汇总、点坐标、分段明细、说明四张工作表
- **浏览手势**：鼠标滚轮缩放、中键拖动平移，触摸板捏合缩放、双指滚动或点击拖动平移
- 自检从 83 项扩到 138 项（新增画圆几何、样式编解码、Excel 包结构校验）

### 1.0.0

- 首个版本：瓦片渲染与浏览、坐标读取、测距、测面积、GeoJSON / KML / CSV 导出

## License

[MIT](LICENSE) © 2026 Thregren
